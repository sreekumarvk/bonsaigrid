[package]
name = "server"
edition.workspace = true
version.workspace = true

[dependencies]
protocol = { path = "../protocol" }
codecs = { path = "../codecs" }
store = { path = "../store" }

[dev-dependencies]
protocol = { path = "../protocol" }
codecs = { path = "../codecs" }
