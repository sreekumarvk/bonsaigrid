[package]
name = "store"
edition.workspace = true
version.workspace = true

[dependencies]
serialization = { path = "../serialization" }
query = { path = "../query" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "hotpath"
harness = false
