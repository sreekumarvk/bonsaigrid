[package]
name = "member"
edition = "2021"
version = "0.0.0"

[dependencies]
store = { path = "../store" }
security = { path = "../security" }
io-uring = "0.6"
libc = "0.2"

[dev-dependencies]
rcgen = { version = "0.13", default-features = false, features = ["ring", "pem"] }
